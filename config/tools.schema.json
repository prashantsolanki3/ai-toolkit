{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "ai-toolkit tools config",
  "type": "object",
  "required": ["version", "tools"],
  "properties": {
    "version": { "type": "string" },
    "tools": {
      "type": "object",
      "additionalProperties": {
        "type": "object",
        "required": [
          "displayName",
          "defaultTarget",
          "assetPaths",
          "assetFormats",
          "supportedAssets"
        ],
        "properties": {
          "displayName": { "type": "string" },
          "defaultTarget": {
            "type": "object",
            "required": ["global", "workspace"],
            "properties": {
              "global": { "type": ["string", "null"] },
              "workspace": { "type": ["string", "null"] }
            }
          },
          "assetPaths": {
            "type": "object",
            "additionalProperties": { "type": "string" }
          },
          "assetFormats": {
            "type": "object",
            "additionalProperties": {
              "type": "object",
              "required": ["filename", "type"],
              "properties": {
                "filename": { "type": "string" },
                "type": { "enum": ["directory", "file"] },
                "sourceFile": {
                  "type": "string",
                  "description": "When converting a directory-format source to a file-format destination, the relative path inside the source dir to copy."
                }
              }
            }
          },
          "notes": {
            "type": "string",
            "description": "Optional notes about the tool — useful when its asset model is uncertain or has caveats."
          },
          "supportedAssets": {
            "type": "array",
            "items": { "type": "string" },
            "minItems": 1
          }
        }
      }
    }
  }
}
