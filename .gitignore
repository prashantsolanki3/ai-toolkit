node_modules/
coverage/
.ai-toolkit-lock.json

# Self-hosted bootstrap trees — generated by `make bootstrap` from source
# assets; do not commit. Re-run `make bootstrap` to refresh.
/.claude/
/.cursor/
/.kiro/
/.gemini/
/.agent/
/.vscode/
# MCP files dropped at the project root by `make bootstrap`
/.mcp.json
# Bootstrap symlinks only — real workflow files under .github/workflows/ ARE committed.
# Exclude the bootstrap-written files but keep the workflows directory.
/.github/copilot-instructions.md
/.github/agents/
/.github/prompts/

test/fixtures/tmp/
*.log
.DS_Store
.env*
/tmp-*
